server:
  port: 8189
  error:
    whitelabel:
      enabled: false

spring:
  application:
    name: northstar-inventory-service
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${AUTH_SERVER_URL:http://localhost:8181/api/auth/oauth2/jwks}
  jpa:
    hibernate.ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5437}/${DB_NAME:northstar_inventory_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:root}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 5
      initialization-fail-timeout: 0

inventory:
  msg: "Welcome to NorthStar Inventory Service"
  build-version: 1.0.0
  base-url: ${BASE_URL:http://localhost:8181}

info:
  app:
    name: Inventory MicroService
    description: NorthStar Retail Inventory Service
    version: 1.0.0

management:
  endpoints:
    web:
      exposure:
        include: "*"
  tracing:
    sampling:
      probability: 1.0

openapi:
  service:
    title: NorthStar Inventory Service Docs
    version: 1.0.0
    url: ${OPENAPI_GATEWAY_URL:http://localhost:8181/api/inventory}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_SERVER_URL:http://localhost:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true
  instance:
    hostname: localhost
    instance-id: ${spring.application.name}:${server.port}
